# requirements_docker.txt — Dependencias para Cloud Run (Gemini API + FastAPI)

# API REST
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Core
python-dotenv>=1.0.0

# LangChain + Google Gemini
langchain-google-genai>=2.0.0
langchain-text-splitters>=0.3.0
langchain-chroma>=1.1.0
langchain-core>=1.2.0
langchain-community>=0.3.0

# PDF / DOCX processing
pypdf>=6.0.0

# Vector DB
chromadb>=1.5.0

# Streamlit (por si se necesita la UI también)
streamlit>=1.40.0

# Visualization
plotly>=5.18.0
pandas>=2.0.0
